export interface Recipe {
        publisher: string,
        ingredients: [],
        source_url: string,
        recipe_id: string,
        image_url: string,
        social_rank: 100,
        publisher_url: string,
        title: string 
}
